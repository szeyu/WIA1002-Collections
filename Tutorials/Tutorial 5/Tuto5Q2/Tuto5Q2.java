/*
 * Click nbfs://nbhost/SystemFileSystem/Templates/Licenses/license-default.txt to change this license
 * Click nbfs://nbhost/SystemFileSystem/Templates/Classes/Class.java to edit this template
 */
package Week5.Tuto5Q2;

/**
 *
 * @author szeyu
 */
public class Tuto5Q2 {
    
    //a)
    /*
    1 else{
        2 Node<E> temp = head;
        3 for(int i=0; i<index; i++){ ---> temp become head.next.next
            4 temp = temp.next;
        5 }
        6 element = temp.element; ---> get the element in the temp node
        7 temp.next.prev = temp.prev; ---> the next temp prev point to the current temp prev
        8 temp.prev.next = temp.next; ---> the prev temp next point to the current temp next
        9 temp.next = null; ---> the current temp next point to null
        10 temp.prev = null; ---> the current temp prev point to null
        11 size --; ---> successfully deleted the node and reduce the size by 1
    12 }

    */
    
    //b)
    
}
